extends ../layout

block content
    div.card-header= cl('Error')
    div.card-body
        p= cl('The requested link is either expired or invalid.')

        if client.metadata.application_url
            p.text-center
                a(href=client.metadata.application_url)= cl('Return to home screen')

        p.text-center
            a(href="/reset-password/request/" + client.client_id)= cl('Reset your password')
